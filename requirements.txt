# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.10.0

# Groq SDK (STT + LLM + TTS)
groq>=0.13.0

# RAG — Embeddings + Vector Search
sentence-transformers>=3.3.0
faiss-cpu>=1.13.0
numpy>=2.0.0

# Audio Processing
# Note: pydub removed (audioop removed in Python 3.13+)
# Groq API handles audio format conversion natively

# Database
# sqlite3 is built-in

# HTTP & Utilities
httpx>=0.27.0
python-dotenv>=1.0.0
tenacity>=9.0.0
structlog>=24.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
